--- core/modules/user/user.module	2022-12-19 15:44:33.533542782 +0000
+++ core/modules/user/user.module	2022-12-19 15:47:16.187883636 +0000
@@ -1310,7 +1319,7 @@
 function user_cookie_save(array $values) {
   $request_time = \Drupal::time()->getRequestTime();
   foreach ($values as $field => $value) {
     // Set cookie for 365 days.
-    setrawcookie('Drupal.visitor.' . $field, rawurlencode($value), $request_time + 31536000, '/');
+    setrawcookie('Drupal.visitor.' . $field, rawurlencode($value), $request_time + 31536000, '/', NULL, TRUE, TRUE);
   }
 }

@@ -1181,7 +1173,8 @@
  *   A cookie name such as 'homepage'.
  */
 function user_cookie_delete($cookie_name) {
-  setrawcookie('Drupal.visitor.' . $cookie_name, '', \Drupal::time()->getRequestTime() - 3600, '/');
+  setrawcookie('Drupal.visitor.' . $cookie_name, '', \Drupal::time()->getRequestTime() - 3600, '/', NULL, TRUE, TRUE);
 }

 /**
