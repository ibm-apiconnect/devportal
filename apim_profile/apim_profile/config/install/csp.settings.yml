report-only:
  enable: false
  directives:
    default-src:
      base: self
  reporting:
    plugin: none
enforce:
  enable: false
  directives:
    default-src:
      base: self
  reporting:
    plugin: none