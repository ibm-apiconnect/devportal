--- /var/aegir/.drush/provision/db/Provision/Service/db/pdo.php.orig	2020-07-30 13:04:32.337343977 +0000
+++ /var/aegir/.drush/provision/db/Provision/Service/db/pdo.php	2020-07-30 13:04:48.707417993 +0000
@@ -20,7 +20,7 @@
     $user = isset($this->creds['user']) ? $this->creds['user'] : '';
     $pass = isset($this->creds['pass']) ? $this->creds['pass'] : '';
     try {
-      $this->conn = new PDO($this->dsn, $user, $pass, array( PDO::MYSQL_ATTR_SSL_CA => '/etc/secrets/portal/cacert.pem', PDO::MYSQL_ATTR_SSL_CIPHER => 'ECDHE-RSA-AES256-GCM-SHA384', PDO::MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => false, PDO::MYSQL_ATTR_SSL_KEY => '/etc/secrets/portal/portal_client_private.key.pem', PDO::MYSQL_ATTR_SSL_CERT => '/etc/secrets/portal/portal_client_public.cert.pem'));
+      $this->conn = new PDO($this->dsn, $user, $pass);
       return $this->conn;
     }
     catch (PDOException $e) {
@@ -86,7 +86,7 @@
     $dsn = $this->dsn . ';dbname=' . $name;
     try {
       // Try to connect to the DB to test if it exists.
-      $conn = new PDO($dsn, $this->creds['user'], $this->creds['pass'], array( PDO::MYSQL_ATTR_SSL_CA => '/etc/secrets/portal/cacert.pem', PDO::MYSQL_ATTR_SSL_CIPHER => 'ECDHE-RSA-AES256-GCM-SHA384', PDO::MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => false, PDO::MYSQL_ATTR_SSL_KEY => '/etc/secrets/portal/portal_client_private.key.pem', PDO::MYSQL_ATTR_SSL_CERT => '/etc/secrets/portal/portal_client_public.cert.pem'));
+      $conn = new PDO($dsn, $this->creds['user'], $this->creds['pass']);
       // Free the $conn memory.
       $conn = NULL;
       return TRUE;
